name: Build & Deploy R binaries

on:
  push:
    branches:
      - stable

permissions:
  contents: write   # Allow clone and write to the repository
  actions: read     # Allow download and execute Marketplace Actions (e.g. upload-artifact)
  packages: write   # Allow read/write GitHub Packages

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
        r-version: ["4.4", "4.5"]

    steps:
    - name: Checkout source
      uses: actions/checkout@v3

    - name: Setup R
      uses: r-lib/actions/setup-r@v2
      with:
        r-version: ${{ matrix.r-version }}
        extra-repositories: "https://inla.r-inla-download.org/R/testing"

    - name: Install build deps
      run: Rscript -e 'install.packages(c("devtools"), repos="https://cloud.r-project.org")'

    - uses: r-lib/actions/setup-r-dependencies@v2
      with:
        dependencies: '"all"'

    - name: Install LLVM for OpenMP (macOS only)
      if: matrix.os == 'macos-latest'
      run: |
        brew install llvm
        brew install libomp

    - name: Configure compiler for LLVM and OpenMP (macOS only)
      if: matrix.os == 'macos-latest'
      run: |
        LLVM_PREFIX=$(brew --prefix llvm)
        echo "PATH=$LLVM_PREFIX/bin:$PATH" >> $GITHUB_ENV
        echo "CC=$LLVM_PREFIX/bin/clang" >> $GITHUB_ENV
        echo "CXX=$LLVM_PREFIX/bin/clang++" >> $GITHUB_ENV
        echo "CFLAGS=-fopenmp -I$LLVM_PREFIX/include" >> $GITHUB_ENV
        echo "CXXFLAGS=-fopenmp -I$LLVM_PREFIX/include" >> $GITHUB_ENV
        echo "LDFLAGS=-fopenmp -L$LLVM_PREFIX/lib -Wl,-rpath,$LLVM_PREFIX/lib" >> $GITHUB_ENV

    - name: Build source package (all platforms)
      run: |
        R CMD build .
      shell: bash

    - name: Install & build binary (Win/macOS only)
      if: matrix.os != 'ubuntu-latest'
      run: |
        R CMD INSTALL --build $(ls -t *.tar.gz | head -n 1)
      shell: bash

    - name: Upload Windows binary
      if: matrix.os == 'windows-latest'
      uses: actions/upload-artifact@v4
      with:
        name: binary-windows-r${{ matrix.r-version }}
        path: "*.zip"

    - name: Upload macOS binary
      if: matrix.os == 'macos-latest'
      uses: actions/upload-artifact@v4
      with:
        name: binary-macos-r${{ matrix.r-version }}
        path: "*.tgz"

    - name: Upload Linux source
      if: matrix.os == 'ubuntu-latest'
      uses: actions/upload-artifact@v4
      with:
        name: source-linux-r${{ matrix.r-version }}
        path: "*.tar.gz"

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
    - name: Checkout gh-pages branch
      uses: actions/checkout@v3
      with:
        ref: gh-pages

    - name: Setup R
      uses: r-lib/actions/setup-r@v2

    - name: Create directory structure
      run: |
        mkdir -p .binaries/bin/windows .binaries/bin/macos .binaries/src/contrib

    # Download all Windows binaries
    - name: Download Windows r4.4
      uses: actions/download-artifact@v4
      with:
        name: binary-windows-r4.4
        path: .binaries/bin/windows/4.4
    - name: Download Windows r4.5
      uses: actions/download-artifact@v4
      with:
        name: binary-windows-r4.5
        path: .binaries/bin/windows/4.5

    # Download all macOS binaries
    - name: Download macOS r4.4
      uses: actions/download-artifact@v4
      with:
        name: binary-macos-r4.4
        path: .binaries/bin/macosx/4.4
    - name: Download macOS r4.5
      uses: actions/download-artifact@v4
      with:
        name: binary-macos-r4.5
        path: .binaries/bin/macosx/4.5

    # Download all Linux source packages
    - name: Download Linux source r4.4
      uses: actions/download-artifact@v4
      with:
        name: source-linux-r4.4
        path: .binaries/src/contrib
    - name: Download Linux source r4.5
      uses: actions/download-artifact@v4
      with:
        name: source-linux-r4.5
        path: .binaries/src/contrib

    # Create .nojekyll (allow dot-folders)
    - name: Create .nojekyll (allow dot-folders)
      run: touch .nojekyll

    - name: Generate PACKAGES indices
      run: |
        Rscript -e 'tools::write_PACKAGES(".binaries/src/contrib", type="source")'
        Rscript -e 'tools::write_PACKAGES(".binaries/bin/windows/4.4", type="win.binary")'
        Rscript -e 'tools::write_PACKAGES(".binaries/bin/windows/4.5", type="win.binary")'
        Rscript -e 'tools::write_PACKAGES(".binaries/bin/macosx/4.4", type="mac.binary")'
        Rscript -e 'tools::write_PACKAGES(".binaries/bin/macosx/4.5", type="mac.binary")'

    - name: Commit & Push
      run: |
        git config user.name  "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add .
        git commit -m "Update binaries $(date +%Y-%m-%d) [skip ci]" || echo "No changes to commit"
        git push origin gh-pages